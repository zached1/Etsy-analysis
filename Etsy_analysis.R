
#First I import the data in, I like file.choose because it always works
etsy <- read.csv(file.choose())

#this lets us take a look at the data we just imported
View(etsy)

#we have three quantitative variables, 